# pg-schema-view

A simple CLI tool to inspect and print a PostgreSQL database schema in a readable format.

## ✨ Features

- Lists all tables in the `public` schema
- Prints columns with data types and nullability
- Works via connection string or env variable
- Great for onboarding and quick schema reviews

## 🚀 Install

```bash
npm install -g pg-schema-view
